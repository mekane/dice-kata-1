{
  "name": "dice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha -b",
    "coverage": "./node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha",
    "view-coverage": "./node_modules/.bin/http-server ./coverage/lcov-report -o -p9999",
    "build": "./node_modules/.bin/browserify -o dist/dice.js -r ./dice -s dice"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "http-server": "^0.9.0",
    "istanbul": "^0.4.4",
    "mocha": "^2.5.3"
  },
  "dependencies": {
    "browserify": "^13.1.0"
  }
}
